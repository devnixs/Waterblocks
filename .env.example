# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=fireblocksreplacement

# Connection String (used by the API)
# This is automatically constructed in docker-compose.yml
# For local development outside Docker, use:
# ConnectionStrings__DefaultConnection=Host=localhost;Database=fireblocksreplacement;Username=postgres;Password=postgres
